“HOI4 Modding Tools”插件（通常指功能最全面的 **`HOI4 Modding Tools`** 或类似的 **`HOI4 Mod Utilities`**），能通过可视化界面极大简化MOD制作流程。核心用法如下：

### 🚀 第一步：安装与配置

1.  **安装插件**：
    *   **推荐方式**：在VS Code扩展商店搜索 “HOI4 Modding Tools” 或 “HOI4 Mod Utilities” 直接安装。
    *   **备用方式**：从GitHub Releases下载`.vsix`文件，在VS Code中按 `Ctrl+Shift+P`，输入 “Install from VSIX” 并选择该文件安装。
2.  **关键配置（最重要的一步）**：
    要让插件正常工作，必须告诉它你的游戏安装位置。
    *   **方法一（命令）**：按 `Ctrl+Shift+P`，输入并执行 “`Select HOI4 install path`” 命令，然后浏览选择你的《钢铁雄心4》游戏根目录。
    *   **方法二（设置）**：按 `Ctrl+,` 打开设置，搜索 `hoi4ModUtilities.installPath` 或 `hoi4.gamePath`，直接填入游戏路径。

### 🎨 第二步：核心功能与可视化编辑器

1.  **打开工具**：在VS Code中打开你的MOD文件夹。点击左侧活动栏的 **“盾牌图标” (Shield icon)**，所有工具会按类别展开。
2.  **核心工具：可视化国策编辑器 (Focus Tree Editor)**
    *   **打开**：点击侧边栏的“**Focus Tree Editor**”，或按 `Ctrl+Shift+P` 输入命令。
    *   **界面**：左侧是国策树列表，中间是**拖拽式画布**，右侧是属性编辑面板。
    *   **创建新树**：点击“**+ New Tree**”，输入国家标签（如 `CHI`）和名称即可。
    *   **添加国策**：在画布上右键选择“Add Focus Here”，或直接按键盘的 **`A` 键**。
    *   **编辑属性**：点击国策节点，在右侧面板修改 `Focus ID`（标识符）、`Cost`（花费天数）、`Prerequisites`（前置要求）、`Completion Reward`（完成奖励）等。
    *   **建立关联**：
        *   **前置要求**：选中国策，点击“**Link Prerequisites**”或按 **`P` 键**，然后依次点击前置国策和当前国策。
        *   **互斥关系**：点击“**Link Exclusives**”或按 **`M` 键**，然后依次点击两个互斥的国策。
    *   **移动与调整**：直接拖拽国策节点即可调整位置，连线会自动更新。

**其他工具速览**：

*   **决议编辑器 (Decision Editor)**：可视化创建和管理游戏中的决议。
*   **文件预览 (Preview)**：可以预览地图、科技树、事件树等。
*   **智能编码辅助 (Hearts of Modding)**：能自动补全代码、显示文档、检查错误、跳转定义，并提供**带颜色预览的本地化编辑**。

### 💡 几个小技巧

*   **从“欢迎”开始**：打开盾牌菜单后，可以先点进“**Welcome / Help**”，里面有个交互式引导。
*   **善用快捷键**：在国策编辑器里，`A`键添加、`P`键设前置、`M`键设互斥、`Delete`键删除，效率很高。
*   **路径必须对**：如果发现预览功能失效或代码无法补全，**优先检查游戏路径设置是否正确**。

如果你是新手，建议从 **“可视化国策编辑器”** 入手，这是最直观能带来成就感的功能。