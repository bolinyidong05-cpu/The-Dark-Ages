country_event = {
    id = "TDA.1"
    title = "TDA_EVENT_1"
    desc = "TDA_EVENT_1_desc"

    # 触发条件
    trigger = {
        # 条件判断
        # is_day = yes
        # has_country_flag = flag_name
    }

    # 平均天数
    mean_time_to_happen = {
        days = 100
    }

    # 选项
    option = {
        name = "TDA_EVENT_1_A"
        effect = {
            # 事件效果
            # add_political_power = 50
            # add_stability = 0.1
        }
    }

    option = {
        name = "TDA_EVENT_1_B"
        effect = {
            # 不同效果
        }
    }
}
